version: "3.8"

services:
  android:
    build: .
    container_name: android_emulator
    ports:
      - "6080:6080"   # Web VNC
      - "5555:5555"   # ADB
    environment:
      - DEVICE=Samsung Galaxy S10
      - WEB_VNC=true
    privileged: true
    restart: unless-stopped
